{
  "$comment": "Qt6 (qtbase + qtmultimedia) is intentionally NOT managed by vcpkg — it is linked against the system Qt6 installation (apt/pacman) to avoid the multi-hour vcpkg rebuild of Qt.",
  "name": "mywave-qt",
  "version": "1.0.0",
  "dependencies": [
    "libsndfile",
    {
      "name": "fftw3",
      "features": ["avx2"]
    }
  ]
}
